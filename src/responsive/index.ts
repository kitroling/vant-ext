import { withInstall } from 'vant/es/utils';
import _Responsive from './Reponsive';

const Responsive = withInstall<typeof _Responsive>(_Responsive);

export default Responsive;
export { Responsive };
